accelerate
blobfile
datasets
httpx[http2]
hypothesis  # test-only: property-based tests (tests/fast/ray/rollout/test_train_data_conversion.py)
mcp[cli]
memray  # needed for debugging (but is lightweight), we can put it to dev mode when using pyproject.toml
omegaconf
pillow
pybase64
pylatexenc
pytest-asyncio
pyyaml
qwen_vl_utils # for VLM
ray[default]
ring_flash_attn; platform_system == "Linux"
sglang-router>=0.2.3
tensorboard
torchft-nightly==2026.4.3; platform_system == "Linux"
transformers
wandb
