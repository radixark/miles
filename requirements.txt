accelerate
backports.strenum; python_version < "3.11"
blobfile
datasets
httpx[http2]
hypothesis>=5.40  # HealthCheck.function_scoped_fixture (used in tests) added in 5.40; ROCm base ships 5.35.1
mcp[cli]
memray  # needed for debugging (but is lightweight), we can put it to dev mode when using pyproject.toml
omegaconf
pillow
polars
pybase64
pylatexenc
pytest-asyncio
pyyaml
qwen_vl_utils # for VLM
ray[default]
ring_flash_attn; platform_system == "Linux"
sglang-router>=0.2.3
tensorboard
torchft-nightly==2026.4.3; platform_system == "Linux"
transformers
wandb
