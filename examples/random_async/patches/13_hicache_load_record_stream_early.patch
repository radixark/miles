diff --git a/python/sglang/srt/managers/cache_controller.py b/python/sglang/srt/managers/cache_controller.py
index e4f8d0b..63513d5 100644
--- a/python/sglang/srt/managers/cache_controller.py
+++ b/python/sglang/srt/managers/cache_controller.py
@@ -677,6 +677,17 @@ class HiCacheController:
         host_indices, device_indices = self.move_indices(op)
         self.write_queue.clear()
 
+        # NOTE: Tag the index tensors on write_stream BEFORE enqueuing the
+        # transfer kernel below. record_stream() makes the caching allocator
+        # aware of write_stream's use, so the underlying storage cannot be
+        # reused by another stream's allocation while the kernel is still
+        # pending. Doing this after the transfer call is too late: the kernel
+        # referencing these indices has already been issued.
+        if host_indices.is_cuda:
+            host_indices.record_stream(self.write_stream)
+        if device_indices.is_cuda:
+            device_indices.record_stream(self.write_stream)
+
         start_event = device_module.Event()
         finish_event = device_module.Event()
 
@@ -687,13 +698,6 @@ class HiCacheController:
                 self.mem_pool_device, host_indices, device_indices, self.io_backend
             )
             finish_event.record()
-            # NOTE: We must save the host indices and device indices here,
-            # this is because we need to guarantee that these tensors are
-            # still alive when the write stream is executing.
-            if host_indices.is_cuda:
-                host_indices.record_stream(self.write_stream)
-            if device_indices.is_cuda:
-                device_indices.record_stream(self.write_stream)
 
         self.ack_write_queue.append(HiCacheAck(start_event, finish_event, op.node_ids))
 
@@ -748,6 +752,15 @@ class HiCacheController:
         producer_event = self.layer_done_counter.events[producer_id]
         producer_event.start_event.record()
 
+        # See the equivalent comment in start_writing(): tag the index
+        # tensors on load_stream BEFORE the per-layer kernels are enqueued
+        # so the caching allocator cannot recycle the underlying storage
+        # out from under an in-flight load.
+        if host_indices.is_cuda:
+            host_indices.record_stream(self.load_stream)
+        if device_indices.is_cuda:
+            device_indices.record_stream(self.load_stream)
+
         with device_module.stream(self.load_stream):
             producer_event.start_event.wait(self.load_stream)
             for i in range(self.layer_num):
@@ -759,13 +772,6 @@ class HiCacheController:
                     self.io_backend,
                 )
                 producer_event.complete(i)
-            # NOTE: We must save the host indices and device indices here,
-            # this is because we need to guarantee that these tensors are
-            # still alive when the load stream is executing.
-            if host_indices.is_cuda:
-                host_indices.record_stream(self.load_stream)
-            if device_indices.is_cuda:
-                device_indices.record_stream(self.load_stream)
 
         self.ack_load_queue.append(
             HiCacheAck(
diff --git a/python/sglang/srt/mem_cache/hybrid_cache/hybrid_cache_controller.py b/python/sglang/srt/mem_cache/hybrid_cache/hybrid_cache_controller.py
index 7d6abda..72abf42 100644
--- a/python/sglang/srt/mem_cache/hybrid_cache/hybrid_cache_controller.py
+++ b/python/sglang/srt/mem_cache/hybrid_cache/hybrid_cache_controller.py
@@ -248,12 +248,42 @@ class HybridCacheController(BaseHiCacheController):
         self.start_writing()
         return host_indices
 
+    @staticmethod
+    def _record_transfer_indices_on_stream(
+        host_indices: torch.Tensor,
+        device_indices: torch.Tensor,
+        pool_transfers: Optional[list[PoolTransfer]],
+        stream,
+    ) -> None:
+        """Tag every index tensor used by an in-flight transfer on ``stream``
+        so the caching allocator cannot recycle the underlying storage before
+        the kernels reading them complete.
+
+        Covers both the merged kv host/device indices and the per-pool
+        ``host_indices``/``device_indices`` carried by ``op.pool_transfers``
+        (e.g. the Mamba HiCache sidecar pool).
+        """
+        for tensor in (host_indices, device_indices):
+            if tensor is not None and tensor.is_cuda:
+                tensor.record_stream(stream)
+        for transfer in pool_transfers or []:
+            for tensor in (transfer.host_indices, transfer.device_indices):
+                if tensor is not None and tensor.is_cuda:
+                    tensor.record_stream(stream)
+
     def start_writing(self) -> None:
         if not self.write_queue:
             return
         op = CacheOperation.merge_ops(self.write_queue)
         host_indices, device_indices = self.move_indices(op)
         self.write_queue.clear()
+        # Tag every index tensor on write_stream BEFORE issuing the backup
+        # kernel below; otherwise the caching allocator may hand the bytes
+        # of a pool_transfer device_indices buffer to another stream while
+        # the backup kernel is still reading from it.
+        self._record_transfer_indices_on_stream(
+            host_indices, device_indices, op.pool_transfers, self.write_stream
+        )
         start_event = device_module.Event()
         finish_event = device_module.Event()
         start_event.record()
@@ -267,10 +297,6 @@ class HybridCacheController(BaseHiCacheController):
                 pool_transfers=op.pool_transfers,
             )
             finish_event.record()
-            if host_indices.is_cuda:
-                host_indices.record_stream(self.write_stream)
-            if device_indices.is_cuda:
-                device_indices.record_stream(self.write_stream)
         self.ack_write_queue.append(HiCacheAck(start_event, finish_event, op.node_ids))
 
     def load(
@@ -316,6 +342,13 @@ class HybridCacheController(BaseHiCacheController):
         self.load_queue.clear()
         producer_event = self.layer_done_counter.events[producer_id]
         producer_event.start_event.record()
+        # Same rationale as start_writing(): record stream ownership BEFORE
+        # the load kernels are enqueued, otherwise the Mamba HiCache load
+        # path can read indices whose backing storage was already reused
+        # for an unrelated allocation on the default stream.
+        self._record_transfer_indices_on_stream(
+            host_indices, device_indices, op.pool_transfers, self.load_stream
+        )
         with device_module.stream(self.load_stream):
             producer_event.start_event.wait(self.load_stream)
             for i in range(self.layer_num):
@@ -328,10 +361,6 @@ class HybridCacheController(BaseHiCacheController):
                     pool_transfers=op.pool_transfers,
                 )
                 producer_event.complete(i)
-            if host_indices.is_cuda:
-                host_indices.record_stream(self.load_stream)
-            if device_indices.is_cuda:
-                device_indices.record_stream(self.load_stream)
         self.ack_load_queue.append(
             HiCacheAck(
                 producer_event.start_event,
